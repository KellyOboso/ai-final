[build]
  command = "pip install -r requirements.txt && python -m gunicorn --bind 0.0.0.0:$PORT app:app"
  publish = "templates"
  functions = "functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
